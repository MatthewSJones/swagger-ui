<div style="padding-top:28px;">
    <h4 style="color: #FFF; opacity:0.88; margin:0; padding-left:15px;">
             <span style="font-variant: small-caps; padding-top:15px;">Microsemi Web Store [ base url</span>: {{basePath}}
            {{#if info.version}}
            , <span style="font-variant: small-caps" data-sw-translate>api version</span>: {{info.version}}
            {{/if}}]
            {{#if validatorUrl}}
            <span style="float:right; padding-right:15px;">
                <a href="{{validatorUrl}}/debug?url={{url}}"><img id="validator" src="{{validatorUrl}}?url={{url}}"></a>
            </span>
            {{/if}}
    </h4>
</div>
